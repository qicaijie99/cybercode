// CyberCode never launches a hidden browser or solves challenges automatically.
// Users may paste a browser-issued cf_clearance cookie as part of their session.
export async function getCfClearanceToken(): Promise<string | null> {
  return null
}
